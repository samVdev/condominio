export interface expenseType {
    id?: string,
    name?: string,
    tower: string,
    mount_dollars: number,
    mount_bs: number,
    dollarBefore: number,
    created?: string,
    service: string,
    image: string
}