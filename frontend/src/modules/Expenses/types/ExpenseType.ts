export interface expenseType {
    id?: string,
    name?: string,
    tower: string,
    facture: boolean,
    mount_dollars: number,
    mount_bs: number,
    dollarBefore: number,
    created?: string,
    service: string,
    image: string
}