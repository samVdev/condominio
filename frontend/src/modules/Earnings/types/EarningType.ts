export interface earningType {
    id?: string,
    name?: string,
    tower: string,
    mount_dollars: number,
    mount_bs: number,
    facture: boolean,
    dollarBefore: number,
    created?: string,
    type: string,
    image: string
}