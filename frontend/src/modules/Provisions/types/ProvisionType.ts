export interface provisionType {
    id?: string,
    name?: string,
    tower: string,
    mount_dollars: number,
    facture: boolean,
    pay: boolean,
    mount_bs: number,
    created?: string,
    service: string,
    month: any
}